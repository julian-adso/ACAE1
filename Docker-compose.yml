services:
  web:
    image: python:3.13-alpine
    build: 
      context: . #"https://github.com/miuguelt/HolaDocker.git"
      dockerfile: Dockerfile
    ports:
      - 8000:8000
    volumes:
      - .:/app
    environment:
      - APP_NAME=$APP_NAME
